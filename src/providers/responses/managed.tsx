export interface ManagedResponse {
    createdAt: Date,
    idManagedWallet: number,
    name: string,
    password: string,
    privateKey: string,
    publicKey: string,
    updatedAt: Date,
    walletId: number,
}